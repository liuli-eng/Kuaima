spring:
  application:
    name: kuaima
  profiles:
    # 切换环境：dev | test
    active: test

jwt:
  secret: suma-backend-jwt-secret-key-0123456789abcdef0123456789
  # access token 有效期（毫秒），3 天
  expiration: 259200000

wechat:
  appid: your-wechat-appid
  secret: your-wechat-secret

kuaima:
  settle:
    # 平台服务费率(% of 工资)，规则待定，默认 0 即暂不收取
    service-fee-rate: 0
