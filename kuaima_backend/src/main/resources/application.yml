spring:
  application:
    name: kuaima
  profiles:
    # 切换环境：dev | test
    active: dev
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 2MB


kuaima:
  settle:
    # 平台服务费率(% of 工资)，规则待定，默认 0 即暂不收取
    service-fee-rate: 0



