# 2026-09-16 工作日志

## 查询：修改之前的 MySQL 地址
用户想找回改动前的数据库连接配置。通过 Git 历史（`E:\Kuaima` 是仓库）还原了 `application-dev.yml` 的 datasource 变更链，结论：

| 提交 | 时间/作者 | url | username/password |
|---|---|---|---|
| `fd3bcba` | 09-05 17:27 ggchzzz "add Im" | `127.0.0.1:3306/kuaima` | root / 11112222 |
| `68440b4` | 09-07 19:02 ggchzzz | 同上 | root / 11112222 |
| `4a88aad` | 09-10 12:04 Hanson | 同上 | root / 11112222 |
| `08b3c79` | 09-14 20:46 Hanson | **`192.168.2.73:3306/kuaima`** | **user / zhUTA5rp228hjKSW** |
| `d209782` | 09-14 23:43 zhouyangbo 解合并冲突 | `127.0.0.1:3306/kuaima` | root / root |
| `c457264` | 09-15 14:54 zhouyangbo | 同上 | root / root（api-docs 改 true） |

- 即「修改之前」的局域网地址是 `192.168.2.73`，账号 `user`；更早则是本机 `127.0.0.1` + `root/11112222`。
- 工作区 yml 与 HEAD **完全一致**（`git status` 无改动）——本次会话未做任何配置写入。

## 顺带发现（未修，已告知用户）
- `application.yml` 的 `spring.profiles.active` 现为 **`test`**（`c457264` 从 dev 改来）。
- `application-test.yml` 的 `springdoc.api-docs.enabled` 仍为 **false**，而 dev 已是 true → 当前激活 test 时 swagger-ui 会 500。需改为 true 才可用。
